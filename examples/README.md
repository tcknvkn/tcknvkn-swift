# Örnekler

Temel kullanım örneği için [basic_usage.swift](basic_usage.swift) dosyasını inceleyebilirsiniz.

Örneği çalıştırmak için SwiftPM içeren bir proje altında aşağıdaki akışı kullanabilirsiniz:

```bash
swift run
```
